import Layout from '../../components/layout/gallery/image'
import { withRouter } from 'next/router'

{/* Import the query object for use in the page. */}

export default withRouter(() => (
  <Layout>
    {/* Content */}
  </Layout>
))
